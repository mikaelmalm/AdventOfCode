import { readFile } from "../../utils";

describe("[05]", () => {
  const exampleInput = 1;

  it("should pass with exampleInput", () => {
    const expectedAnswer = 1;
    const actualAnswer = 1;

    expect(actualAnswer).toEqual(expectedAnswer);
  });
});
